import React from 'react';

const Form = () => {
    return(
        <div>
            <form>
                <input type="text" placeholder="Search Countries!"/>
                <input type="submit"/>
                <button>All Countries</button>
                <button>Quiz</button>
            </form>
        </div>
    )
}

export default Form;